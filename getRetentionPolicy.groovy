def jobs = Jenkins.instance.getAllItems() // Get all jenkins items  //Headerprintln ("Job Name,URL,Days to Keep,Number to keep,Artifact Days to keep,Artifact Num. to keep")jobs.each { job ->    template = "" // Verify that the item contains the method getLastBuild (avoids templates and folders)   if (job.metaClass.getMetaMethod("getLastBuild")) {        // Get Retention policy  def d = job.buildDiscarder       // Print Line with information found       if (d){          println("${job.name},${job.absoluteUrl},${d.daysToKeep},${d.numToKeep},${d.artifactDaysToKeep},${d.artifactNumToKeep}")        }else{          println("${job.name},${job.absoluteUrl},No Retention Policy")        }    }}println ""
